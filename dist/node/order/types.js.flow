/* @flow strict */

import type {
  UUID,
  Instant,
} from '../core'

export type Id = UUID

type Base = {}

type BaseEntry = Base & {}

export type Entry = BaseEntry & {}

export type IndexedEntry = BaseEntry & {}
